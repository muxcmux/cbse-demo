<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cbse</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <link href='http://fonts.googleapis.com/css?family=Brawler' rel='stylesheet' type='text/css'>

    <!-- Le fav and touch icons -->
    <link href="/favicon.ico" rel="shortcut icon">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          
          <a class="brand" href="/">CBSE</a>
          <div class="container nav-collapse">
            <ul class="nav pull-right">
              <li><%= link_to '<i class="icon-off icon-white"></i> Logout'.html_safe, destroy_admin_session_path, method: :delete %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
          <div class="span3">
            <div class="well" style="padding: 8px 0;">
              <ul class="nav nav-list">
                <li class="nav-header">home</li>
                <%= sidebar_link 'Management', management_path, icon: :home, c: :management , a: :index %>
                <li class="nav-header">user management</li>
                <%= sidebar_link "View all users", users_path, icon: 'eye-open', c: :users, a: :index %>
                <%= sidebar_link "Create a new user", new_user_path, icon: :pencil, c: :users, a: :new %>
                <li class="divider"></li>
                <li class="nav-header">account management</li>
                <%= sidebar_link 'View all accounts', accounts_path, icon: 'eye-open', c: :accounts, a: :index %>
                <%= sidebar_link 'Create new account', new_account_path, icon: :pencil, c: :accounts, a: :new %>
                <li class="divider"></li>
                <li class="nav-header">transaction management</li>
                <%= sidebar_link 'See recent transactions', '#', icon: 'eye-open', c: :transations, a: :index %>
                <%= sidebar_link 'Create a transaction', '#', icon: :pencil, c: :transactions, a: :new %>
                <li class="divider"></li>
                <%= sidebar_link 'Logout', destroy_admin_session_path, icon: :off, method: :delete %>
              </ul>
            </div>
          </div>
          <div class="span9">
            <%= yield %>
          </div>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; Anthony Karasavov K1159488, <%= Time.now.year %></p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
